export type Service = {
    metricsUrl: string,
    hostname: string,
    ipv4: string,
    ipv6: string,
    asName: string,
    bandwidth: number,
    offline: boolean,
    isMultiInstance: boolean,
    countryCode: string,
}
